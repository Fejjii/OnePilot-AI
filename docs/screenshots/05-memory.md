# Placeholder — Memory

**Target file:** `05-memory.png` (gitignored)  
**URL:** `/memory`  
**Viewport:** 1440×900  

**Must include:** Memory UI controls/list. On public demo, prefer a frame that shows shared-demo isolation / agent memory disabled if the UI surfaces it.  
**Story:** Durable memory exists for private tenants; shared demo prevents cross-reviewer leakage.
